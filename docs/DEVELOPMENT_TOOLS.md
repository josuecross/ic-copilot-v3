# Development Tools

Current phase: Phase 1.36C.

The active product tools are top-level scripts and CLI product commands. Historical archive utilities have been removed from the active repo. `scripts/run_product_latency_smoke.py` is the live bounded-runtime smoke report for Phase 1.36C, including final brief quality and blocker-reselection metadata where available.

Do not reintroduce dev archive material into the product runtime, default web app, normal CLI help, or runtime knowledge path.

Keep new tools product-focused unless they are clearly isolated outside the product path.
